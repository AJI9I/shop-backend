# Автоматическое исправление ошибки сборки

## ✅ Решение готово!

Я создал автоматический скрипт для исправления проблемы.

## 🚀 Запуск скрипта

**Вариант 1 (рекомендуется):**
1. Откройте проводник Windows
2. Перейдите в папку: `C:\IdeaPRG\miners\shop-backend\`
3. Двойной клик по файлу: **`stop-and-rebuild.bat`**

**Вариант 2 (через командную строку):**
1. Откройте командную строку (cmd)
2. Выполните:
   ```cmd
   cd C:\IdeaPRG\miners\shop-backend
   stop-and-rebuild.bat
   ```

## 📋 Что делает скрипт:

1. ✅ Останавливает все Java процессы (java.exe, javaw.exe)
2. ✅ Удаляет директорию `target`
3. ✅ Выполняет `mvn clean`
4. ✅ Выполняет `mvn compile -DskipTests`

## ⚠️ Если скрипт не сработал:

1. **Закройте IDE** (IntelliJ IDEA, Eclipse и т.д.)
2. **Откройте командную строку (cmd)** и выполните:
   ```cmd
   cd C:\IdeaPRG\miners\shop-backend
   taskkill /F /IM java.exe
   taskkill /F /IM javaw.exe
   timeout /t 2
   rmdir /s /q target
   mvn clean compile -DskipTests
   ```

## 📁 Созданные файлы:

- `stop-and-rebuild.bat` - автоматический скрипт исправления
- `README_BUILD_FIX.md` - подробная инструкция
- `QUICK_FIX.txt` - краткая инструкция
- `INSTRUCTIONS.md` - этот файл




